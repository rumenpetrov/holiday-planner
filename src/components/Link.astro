---
import { localizePath } from 'astro-i18next';

export interface Props {
  href: string;
  className?: string,
};

const {
  href,
  className,
} = Astro.props;
---
<a href={localizePath(href)} rel="prefetch" class:list={['root', className]}>
  <slot />
</a>

<style>
  .root {
    color: var(--palette-primary);
    transition: all 0.3s ease;
  }

  .root:hover {
    text-decoration: none;
  }
</style>
